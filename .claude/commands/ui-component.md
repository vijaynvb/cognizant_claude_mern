Create a new UI component named [component_name] in the `components/ui/` folder.

Use the following summary for implementation details:
[summary]

- Use PascalCase for file and folder names
- Create functional React component
- Add variants, sizes, and test coverage
- Use theme colors from @styles/theme.css
- Add the component to the preview page only